<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="shoppingList.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:shoppingList"
    xmlns:views="clr-namespace:shoppingList.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="shoppingList"
    Shell.TabBarBackgroundColor="#1f1f1f">
    <TabBar>
        <ShellContent
            Title="Lista zakupów"
            ContentTemplate="{DataTemplate views:ShoppingList}" />
        <ShellContent
            Title="Przepisy"
            ContentTemplate="{DataTemplate views:Recipes}" />
        <ShellContent
            Title="Sklepy"
            ContentTemplate="{DataTemplate views:Shops}" />
    </TabBar>

</Shell>
